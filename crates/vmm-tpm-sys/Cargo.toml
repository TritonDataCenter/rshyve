[package]
name = "vmm_tpm_sys"
version = "0.1.0"
edition = "2021"
rust-version.workspace = true
license = "MPL-2.0"
build = "build.rs"
links = "tpms"
description = "FFI bindings + build glue for vendored libtpms (TPM 2.0)"

[lints]
workspace = true

[dependencies]
libc.workspace = true
